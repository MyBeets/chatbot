import getResponse
import learnFrom
class bot:
	def __init__(self, name, size, layers):
		self.name = name
		self.size = size
		self.layer_num = layer_num
		self.lines = 0
		learnFrom.new()
	def getResponse(input):
		return getResponse.getResponse(input)

	def learnFrom(conversation):
		learnFrom.learnFrom(bot, conversation)
